[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn -w 1 --threads 50 --bind 0.0.0.0:$PORT server:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "examguard-backend"
